# 📚 Management API

### 🧩 Описание проекта
**Management API** — это серверное приложение на **FastAPI**, предоставляющее REST API для управления школьной системой.  
Проект поддерживает работу с данными о **учениках, учителях, предметах, оценках и занятиях**, обеспечивая безопасное и удобное взаимодействие между клиентом и базой данных.

---

## ⚙️ Основные возможности
- 👩‍🏫 Управление учителями и предметами  
- 🎓 Управление учениками и классами  
- 🧮 Учёт оценок и уроков  
- 💾 Подключение к базе данных (через SQLAlchemy)  
- ⚡ Высокая производительность с FastAPI  
- 🔐 Гибкое взаимодействие через JSON API

---

## 🛠 Технологии
- **Python 3.11+**  
- **FastAPI** — основной фреймворк  
- **SQLAlchemy** — ORM для базы данных  
- **Uvicorn** — сервер запуска  
- **Pydantic** — валидация данных  
- **Redis (опционально)** — кэширование  
- **PostgreSQL / SQLite** — основное хранилище данных

---

## 🚀 Установка и запуск

```bash
# Клонировать репозиторий
git clone https://github.com/your-username/school-api.git
cd school-api/api

# Создать и активировать виртуальное окружение
python -m venv venv
source venv/bin/activate  # или venv\Scripts\activate на Windows

# Установить зависимости
pip install -r requirements.txt

# Запустить сервер
uvicorn main:app --reload
```

API будет доступно по адресу:  
👉 http://127.0.0.1:8000  

---

## 📡 Основные эндпоинты

| Метод | Путь | Описание |
|-------|------|-----------|
| `GET` | `/api/student` | Получить список учеников |
| `POST` | `/api/student` | Добавить нового ученика |
| `GET` | `/api/teacher` | Список учителей |
| `POST` | `/api/teacher` | Добавить нового учителя |
| `GET` | `/api/lesson` | Список занятий |
| `POST` | `/api/mark` | Добавить оценку ученику |
| `GET` | `/api/session` | Сессии/авторизация пользователей |

---

## 📂 Структура проекта
```
api/
 ├── main.py                  # Точка входа FastAPI
 ├── app.py                   # Инициализация приложения
 ├── config.py                # Настройки окружения
 ├── api/
 │    ├── endpoints/          # REST эндпоинты
 │    ├── schemas/            # Pydantic-схемы
 ├── database/
 │    ├── controllers/        # CRUD-логика
 │    ├── cache.py            # Кэширование
 │    ├── database.py         # Подключение к БД
```

---

## 🧪 Пример запроса

**POST /api/student**

```json
{
  "name": "Иван Петров",
  "age": 14,
  "class_id": 2
}
```

**Ответ:**
```json
{
  "id": 15,
  "name": "Иван Петров",
  "age": 14,
  "class_id": 2
}
```
---

## 👨‍💻 Автор
**r0kki1**  
🌐 [GitHub Profile](https://github.com/r0kki1)
# Test update
